/**
 * WorkflowDefinitionDTO — the wire/transport format for WorkflowDefinition.
 * Re-exported from @local/builder
 */
export type { WorkflowDefinitionDTO } from '@local/builder';
export { CURRENT_SCHEMA_VERSION, toDTO, fromDTO } from '@local/builder';
